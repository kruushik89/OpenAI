# Practice: flight assistant — практичне завдання

У цій лекції автор пропонує **проєкт-вправу** — асистента для пошуку та бронювання рейсів. Це інтеграційна вправа: поєднати **історію діалогу** з **двома tools** (пошук рейсів і бронювання) у єдиний сценарій чату з OpenAI API.

---

## Мета проєкту

Реалізувати **діалог**, у якому можна:

1. **Запитати рейси** між двома пунктами призначення.
2. **Забронювати рейс**, якщо він доступний.

---

## Сценарій діалогу

### Пошук рейсів

Користувач запитує рейси між двома містами/аеропортами. Пункти вказуються **кодами аеропортів** (3 або 4 літери — точна довжина не критична).

Система відповідає списком рейсів: **один, два або жодного** — залежно від наявності.

### Бронювання

Якщо рейси є, користувач може попросити:

> «Please make a reservation for that flight»

Система має повернути **номер бронювання**. Якщо бронювання неможливе — повідомити про це.

---

## Що потрібно для реалізації

Усе необхідне вже розглянуто в **цьому розділі** та **попередньому**:

| Компонент | Звідки |
|-----------|--------|
| **Діалог з історією** | Попередній розділ |
| **Tool calls / functions** | Поточний розділ (лекції 23–25) |

Потрібно **дві функції (tools)**:

1. **`findFlights`** — знаходить рейси між **origin** і **destination** (коди аеропортів).
2. **`reserveFlight`** — бронює рейс і повертає **номер бронювання**; якщо неможливо — повідомляє про це.

---

## Рішення

### 1. Мок-дані рейсів

Замість реальної бази даних використовуємо масив `flights` з кількома маршрутами:

| Маршрут | Рейси | Результат пошуку |
|---------|-------|------------------|
| JFK → LAX | AA101, UA205 | **2 рейси** |
| JFK → SFO | DL310 | **1 рейс** |
| LAX → JFK | — | **0 рейсів** |
| ORD → MIA | SW420 (недоступний) | **0 рейсів** (фільтр `available`) |

### 2. Функції `findFlights` і `reserveFlight`

- **`findFlights`** — фільтрує рейси за `origin` і `destination`, повертає JSON з масивом `flights` або повідомленням про відсутність рейсів.
- **`reserveFlight`** — шукає рейс за `flightNumber`; якщо знайдено і доступний — генерує номер бронювання (`RES-...`) і позначає рейс як заброньований (`available: false`).

### 3. Два tools з параметрами

```ts
const tools: OpenAI.Chat.Completions.ChatCompletionTool[] = [
  {
    type: "function",
    function: {
      name: "findFlights",
      description: "Find available flights between two airports",
      parameters: {
        type: "object",
        properties: {
          origin: {
            type: "string",
            description: "The origin airport code (3 or 4 letters)",
          },
          destination: {
            type: "string",
            description: "The destination airport code (3 or 4 letters)",
          },
        },
        required: ["origin", "destination"],
      },
    },
  },
  {
    type: "function",
    function: {
      name: "reserveFlight",
      description: "Reserve a flight and return a reservation number",
      parameters: {
        type: "object",
        properties: {
          flightNumber: {
            type: "string",
            description: "The flight number to reserve",
          },
        },
        required: ["flightNumber"],
      },
    },
  },
];
```

### 4. Інтерактивний діалог з історією

На відміну від одноразового виклику в `tools/index.ts`, тут використовується **цикл чату** через `readline`:

- Кожне повідомлення користувача додається в `context`.
- Викликається `createChatCompletion()`, який обробляє tool calls у циклі `while`.
- Фінальна відповідь асистента зберігається в контексті — тому фраза *«that flight»* коректно посилається на попередній пошук.

### 5. Цикл обробки tool calls

```ts
async function createChatCompletion() {
  while (true) {
    const response = await openai.chat.completions.create({
      model: "gpt-4o-mini",
      messages: context,
      tools,
      tool_choice: "auto",
    });

    const message = response.choices[0].message;
    const willInvokeFunction =
      response.choices[0].finish_reason === "tool_calls";

    if (willInvokeFunction && message.tool_calls) {
      context.push(message);

      for (const toolCall of message.tool_calls) {
        const toolResponse = handleToolCall(toolCall);
        if (toolResponse === null) continue;

        context.push({
          role: "tool",
          content: toolResponse,
          tool_call_id: toolCall.id,
        });
      }

      continue; // другий (або наступний) запит до API
    }

    context.push(message);
    console.log(`assistant: ${message.content}`);
    break;
  }
}
```

> **Ключова відмінність від лекцій 24–25:** цикл `while` всередині `createChatCompletion` дозволяє обробляти tool calls **без обмеження кількості раундів** — модель може викликати `findFlights`, отримати результат, а потім у наступному повідомленні користувача викликати `reserveFlight`, використовуючи контекст попереднього діалогу.

---

## Повний код `tools/flight-assistant.ts`

```ts
import OpenAI from "openai";
import * as readline from "readline/promises";

const openai = new OpenAI();

interface Flight {
  flightNumber: string;
  origin: string;
  destination: string;
  departure: string;
  arrival: string;
  available: boolean;
}

const flights: Flight[] = [
  {
    flightNumber: "AA101",
    origin: "JFK",
    destination: "LAX",
    departure: "08:00",
    arrival: "11:30",
    available: true,
  },
  {
    flightNumber: "UA205",
    origin: "JFK",
    destination: "LAX",
    departure: "14:00",
    arrival: "17:30",
    available: true,
  },
  {
    flightNumber: "DL310",
    origin: "JFK",
    destination: "SFO",
    departure: "09:30",
    arrival: "12:45",
    available: true,
  },
  {
    flightNumber: "SW420",
    origin: "ORD",
    destination: "MIA",
    departure: "10:00",
    arrival: "14:00",
    available: false,
  },
];

function findFlights(origin: string, destination: string): string {
  const matches = flights.filter(
    (flight) =>
      flight.origin.toUpperCase() === origin.toUpperCase() &&
      flight.destination.toUpperCase() === destination.toUpperCase() &&
      flight.available,
  );

  if (matches.length === 0) {
    return JSON.stringify({
      flights: [],
      message: "No available flights found between the specified airports.",
    });
  }

  return JSON.stringify({ flights: matches });
}

function reserveFlight(flightNumber: string): string {
  const flight = flights.find(
    (item) => item.flightNumber.toUpperCase() === flightNumber.toUpperCase(),
  );

  if (!flight) {
    return JSON.stringify({
      success: false,
      message: "Flight not found. Cannot make a reservation.",
    });
  }

  if (!flight.available) {
    return JSON.stringify({
      success: false,
      message: "This flight is no longer available for reservation.",
    });
  }

  const reservationNumber = `RES-${Date.now().toString(36).toUpperCase()}`;
  flight.available = false;

  return JSON.stringify({
    success: true,
    reservationNumber,
    flightNumber: flight.flightNumber,
    origin: flight.origin,
    destination: flight.destination,
  });
}

const tools: OpenAI.Chat.Completions.ChatCompletionTool[] = [
  {
    type: "function",
    function: {
      name: "findFlights",
      description: "Find available flights between two airports",
      parameters: {
        type: "object",
        properties: {
          origin: {
            type: "string",
            description: "The origin airport code (3 or 4 letters)",
          },
          destination: {
            type: "string",
            description: "The destination airport code (3 or 4 letters)",
          },
        },
        required: ["origin", "destination"],
      },
    },
  },
  {
    type: "function",
    function: {
      name: "reserveFlight",
      description: "Reserve a flight and return a reservation number",
      parameters: {
        type: "object",
        properties: {
          flightNumber: {
            type: "string",
            description: "The flight number to reserve",
          },
        },
        required: ["flightNumber"],
      },
    },
  },
];

const context: OpenAI.Chat.ChatCompletionMessageParam[] = [
  {
    role: "system",
    content:
      "You are a helpful flight assistant. You help users find flights between airports and make reservations. Airport codes are 3 or 4 letters. When listing flights, include the flight number, departure and arrival times. When a reservation succeeds, clearly share the reservation number.",
  },
];

function handleToolCall(
  toolCall: OpenAI.Chat.Completions.ChatCompletionMessageToolCall,
): string | null {
  if (toolCall.type !== "function") return null;

  const toolName = toolCall.function.name;

  if (toolName === "findFlights") {
    const { origin, destination } = JSON.parse(toolCall.function.arguments);
    return findFlights(origin, destination);
  }

  if (toolName === "reserveFlight") {
    const { flightNumber } = JSON.parse(toolCall.function.arguments);
    return reserveFlight(flightNumber);
  }

  return null;
}

async function createChatCompletion() {
  while (true) {
    const response = await openai.chat.completions.create({
      model: "gpt-4o-mini",
      messages: context,
      tools,
      tool_choice: "auto",
    });

    const message = response.choices[0].message;
    const willInvokeFunction =
      response.choices[0].finish_reason === "tool_calls";

    if (willInvokeFunction && message.tool_calls) {
      context.push(message);

      for (const toolCall of message.tool_calls) {
        const toolResponse = handleToolCall(toolCall);

        if (toolResponse === null) continue;

        context.push({
          role: "tool",
          content: toolResponse,
          tool_call_id: toolCall.id,
        });
      }

      continue;
    }

    context.push(message);
    console.log(`assistant: ${message.content}`);
    break;
  }
}

async function main() {
  const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout,
  });

  console.log("Flight assistant ready. Type your message (Ctrl+C to exit).");
  console.log("Example: Find flights from JFK to LAX");

  while (true) {
    let userInput: string;

    try {
      userInput = (await rl.question("user: ")).trim();
    } catch {
      break;
    }

    if (!userInput) continue;

    context.push({
      role: "user",
      content: userInput,
    });

    await createChatCompletion();
  }

  rl.close();
}

main();
```

---

## Запуск

У `package.json` додано скрипт:

```json
"start:flight": "node -r ts-node/register --env-file=.env src/tools/flight-assistant.ts"
```

```bash
cd chat-app
npm run start:flight
```

---

## Результат

| Запит | Результат |
|-------|-----------|
| *Find flights from JFK to LAX* | Список з **2 рейсів** (AA101, UA205) |
| *Find flights from JFK to SFO* | Список з **1 рейсу** (DL310) |
| *Find flights from LAX to JFK* | **Немає доступних рейсів** |
| *Please make a reservation for that flight* (після пошуку) | Номер бронювання, напр. **RES-MREX56MG** |
| Повторне бронювання того ж рейсу | Повідомлення, що рейс **більше недоступний** |

### Приклад діалогу

```
user: Find flights from JFK to LAX
assistant: Here are the available flights from JFK to LAX:
           1. Flight Number: AA101 — Departure: 08:00, Arrival: 11:30
           2. Flight Number: UA205 — Departure: 14:00, Arrival: 17:30

user: Please make a reservation for that flight
assistant: Your reservation for flight AA101 has been successfully made!
           Reservation Number: RES-MREX56MG
```

---

## Підсумок

**Flight assistant** поєднує три концепції з попередніх лекцій:

1. **Інтерактивний чат** з накопиченням історії (`context`).
2. **Tools з параметрами** — `findFlights(origin, destination)` і `reserveFlight(flightNumber)`.
3. **Цикл обробки tool calls** — модель викликає функції, отримує результати і формує відповідь користувачу.

Ваше рішення може відрізнятися (інші мок-дані, інший спосіб генерації номера бронювання, `process.stdin` замість `readline`) — це нормально, головне — коректний цикл tool calls і збереження контексту діалогу.
